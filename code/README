create python env just one 
```python3 -m venv venv```

installer les librairies 
```python3 -m pip install --break-system-packages -r requirement.txt```
sudo apt install cifs-utils

activate python env 
```source venv/bin/activate```